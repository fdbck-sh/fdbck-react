/**
 * Set this to a valid response token from your fdbck dashboard.
 * Generate one via: POST /v1/questions/:id/token
 */
export const FDBCK_TOKEN = 'Hx90bX_E9vKegLQd';

/**
 * Base URL of the fdbck API server.
 * Change this to point at staging or production as needed.
 */
export const FDBCK_BASE_URL = 'http://localhost:3000';
